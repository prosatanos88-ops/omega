// botModules.ts

// Configuration and definitions for APEX modules.

const APEX_MODULES = {
  ZERO: {
    enabled: true,
    config: { /* Configuration for ZERO module */ }
  },
  ARSENAL: {
    enabled: true,
    config: { /* Configuration for ARSENAL module */ }
  },
  HUNTER: {
    enabled: true,
    config: { /* Configuration for HUNTER module */ }
  },
  VISION: {
    enabled: true,
    config: { /* Configuration for VISION module */ }
  },
  PEGASUS: {
    enabled: true,
    config: { /* Configuration for PEGASUS module */ }
  },
  OMEGA: {
    enabled: true,
    config: { /* Configuration for OMEGA module */ }
  }
};

export default APEX_MODULES;